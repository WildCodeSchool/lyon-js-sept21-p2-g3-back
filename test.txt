      if (!ingredientsInDB.map((iDB) => iDB.id).includes(i.foodId)) {
        const ingredient = {
          id: i.foodId,
          name: i.food,
          measure: i.measure,
          category: i.foodCategory,
          image: i.image,
        };
        return ingredient;
      }